# Claude Code
.claude/
CLAUDE.md
AGENTS.md

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key
secrets.*
credentials.*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
*.swp
*.swo
*~
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies / build
node_modules/
dist/
build/
.cache/

# Camera originals — keep them in the group's own archive, never in the repo.
# tools/build-gallery.py reads them from here and commits only the derivatives.
assets/events/*/originals/
